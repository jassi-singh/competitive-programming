#include <bits/stdc++.h>
using namespace std;
#define ceil(x, y) ceil((double)x / (double)y)
#define ll long long
void solve()
{
    int t = 1;
    cin >> t;
    while (t--)
    {
        int m,n;
        cin>>m>>n;
        map<int,int> stones,obs;
        for (int i = 0; i < m; i++)
        {
            string s;
            cin>>s;
            for (int j = 0; j < n; j++)
            {
                switch (s[j])
                {
                case 'o':
                    obs[]
                    break;
                
                default:
                    break;
                }
            }
            
        }
        
    }
}
int main() 
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}